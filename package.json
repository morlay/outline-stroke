{
  "name": "outline-stroke",
  "version": "0.0.0",
  "description": "Outline SVG stroke to closed svg path",
  "main": "lib/index.js",
  "types": "lib/index.d.ts",
  "files": [
    "@types/*",
    "src/*",
    "lib/*"
  ],
  "license": "WTFPL",
  "ava": {
    "files": [
      "lib/__tests__/*.spec.js"
    ]
  },
  "scripts": {
    "lint": "tslint src/{,**/}*.ts{,x} -c tslint.json",
    "test": "rm -rf lib && tsc && ava -v",
    "prepublish": "npm run test"
  },
  "devDependencies": {
    "@types/fs-extra": "0.0.x",
    "ava": "0.17.x",
    "fs-extra": "1.0.x",
    "tslint": "4.2.x",
    "typescript": "2.1.x"
  },
  "dependencies": {
    "@types/lodash": "4.14.x",
    "bezier-js": "2.2.x",
    "lodash": "4.17.x",
    "svgpath": "2.2.x"
  }
}
